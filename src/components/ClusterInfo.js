import React from 'react';

const ClusterInfo = ({districtName}) => (
  <div>
    <p style={{color: '#fff', fontSize: 13, margin: 0, padding: 4}}>{districtName} District</p>
  </div>
);

export default ClusterInfo;
